tracker_type: bytetrack
track_high_thresh: 0.25 # was 0.20 — low-conf jittery detections ko primary se bahar rakho
track_low_thresh: 0.10 # was 0.05 — extremely noisy detections ignore karo
new_track_thresh: 0.15
track_buffer: 150
match_thresh: 0.65
fuse_score: true
